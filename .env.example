# ---- Supabase ----
NEXT_PUBLIC_SUPABASE_URL="https://xxxxxxxxxxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOi..."
# Solo servidor. NUNCA exponer con prefijo NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=""

# ---- Sitio ----
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_WHATSAPP="5213141502203"

# ---- Acceso al panel /admin ----
# Ya no hay variables aqui: el login de /admin usa Supabase Auth de verdad
# (supabase.auth.signInWithPassword). El usuario admin se crea una vez en
# el dashboard de Supabase (Authentication -> Users) y se marca su
# `profiles.role` como 'admin' o 'tecnico' — ver DEPLOY_HOSTINGER.md,
# paso 3, y src/lib/admin-auth.ts para el detalle completo.
